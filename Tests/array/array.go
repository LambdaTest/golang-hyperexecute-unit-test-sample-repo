package array

func sum(numbers []int) int {
    total := 0
    for _, number := range numbers {
        total += number
    }
    return total
}

func sum1(numbers []int) int {
    total := 0
    for _, number := range numbers {
        total += number
    }
    return total
}